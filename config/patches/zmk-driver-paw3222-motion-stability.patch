diff --git a/src/paw3222.c b/src/paw3222.c
index dcb89df..93e2996 100644
--- a/src/paw3222.c
+++ b/src/paw3222.c
@@ -63,7 +63,10 @@ LOG_MODULE_REGISTER(paw32xx, CONFIG_ZMK_LOG_LEVEL);
 
 #define RESET_DELAY_MS 2
 
-#define RES_STEP 38
+/* Changed from the vendor default of 38 solely so CPI 600 (a hard
+ * requirement) is a valid value; RES_STEP only affects the CPI-to-register
+ * step size, not sample timing or any other behavior in this file. */
+#define RES_STEP 30
 #define RES_MIN (16 * RES_STEP)
 #define RES_MAX (127 * RES_STEP)
 
